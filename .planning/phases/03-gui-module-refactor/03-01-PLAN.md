---
phase: 03-gui-module-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gui_interface.py
  - categorizer.py
  - config.json
  - test_gui.py
  - test_phase1_integration.py
autonomous: true
requirements:
  - UI-04
user_setup: []

must_haves:
  truths:
    - "自动分类路径下逐笔刷新不再 destroy/recreate 整个交易信息面板"
    - "分类按钮面板首次构建后可复用，非必要时不全量重建"
    - "add_classified_transaction 增量维护索引，无 O(n) 全量重算"
    - "进度更新按 progress_interval 节流"
    - "Phase 1 线程安全模式保持不变（run_on_main_thread）"
  artifacts:
    - path: gui_interface.py
      provides: "_transaction_info_labels 复用或 _update_transaction_labels"
    - path: gui_interface.py
      provides: "_classification_menu_built 或等价缓存标志"
    - path: gui_interface.py
      provides: "add_classified_transaction 增量 index 更新"
    - path: test_gui.py
      provides: "test_ui_perf_no_full_rebuild_on_auto_classify"
  key_links:
    - from: categorizer.py
      to: gui_interface.py
      via: "run_on_main_thread 包装 UI 更新（保持不变）"
      pattern: "run_on_main_thread"
---

## Phase Goal

**As a** 账单分类用户, **I want to** 逐笔分类时界面流畅不卡顿, **so that** 处理大账单时体验舒适。

## 多源覆盖审计

| 来源 | 条目 | 覆盖 |
|------|------|------|
| 用户反馈 | 每笔刷新卡顿 | Task 1-4 |
| REQ | UI-04 | 全部 Task |
| Phase 1 | ARCH-04 线程安全 | 验证不回归 |

<objective>
优化 gui_interface.py 中逐笔 UI 刷新策略，消除 destroy/recreate 与 O(n) 重算，利用 config display.progress_interval 节流。

Purpose: 交付用户可感知的流畅性提升，为 Wave 2 模块拆分减少改动面。
Output: 复用型 transaction 面板、分类菜单缓存、增量 Treeview 索引、节流进度条。
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-gui-module-refactor/RESEARCH.md
@.planning/phases/01-fix-gui-thread-safety/01-02-SUMMARY.md
@gui_interface.py
@categorizer.py
@config.json
@test_gui.py
@test_phase1_integration.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 交易信息面板 Label 复用</name>
  <files>gui_interface.py, test_gui.py</files>
  <read_first>
    - gui_interface.py _create_transaction_window、_display_transaction_impl（约 L753-807）
  </read_first>
  <behavior>
    - Test: 连续两次 _display_transaction_impl 后 transaction_info_frame 子控件数量不增加（无累积 destroy/create）
  </behavior>
  <action>
    1. 在 _create_transaction_window 中预创建固定 Label 组（title、detail 多行或单 Text 控件），存为 self._txn_labels 或 self._txn_detail_text
    2. _display_transaction_impl 改为 config(text=...) / delete+insert END，禁止每笔 destroy 全部 children
    3. 去掉 _display_transaction_impl 末尾 update_idletasks；改 root.after_idle 批量刷新（可选，仅当 progress 也更新时）
  </action>
  <verify>
    <automated>pytest test_gui.py::test_transaction_panel_reuses_widgets -x -q</automated>
  </verify>
  <done>自动分类连续 10 笔后 widget 数量稳定</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 分类菜单缓存与增量 Treeview</name>
  <files>gui_interface.py, test_gui.py</files>
  <action>
    1. display_classification_menu：若 base_categories 与 suggestions 结构未变且面板已构建，跳过 destroy；仅更新高亮/启用状态
    2. 若 categories 变化（用户新增分类 n），仅增量追加按钮
    3. add_classified_transaction：新条目 index=0，仅更新受影响 entry 的 index，禁止每次遍历全量 classified_data 重建 tree_item_to_index
    4. 自动分类（未调用 display_classification_menu）路径零分类面板重建
  </action>
  <verify>
    <automated>pytest test_gui.py::test_classified_tree_incremental_index -x -q</automated>
  </verify>
  <done>100 笔 mock 自动分类无 classification_frame destroy 调用</done>
</task>

<task type="auto">
  <name>Task 3: 进度节流与自动分类批量刷新</name>
  <files>gui_interface.py, categorizer.py, config.json</files>
  <action>
    1. GUIInterface 读取 config display.progress_interval（默认 10）
    2. _display_progress_impl：仅当 idx % interval == 0 或 idx == total 时更新 progressbar
    3. categorizer 自动分类路径：add_classified_transaction 后不必每笔 run_on_main_thread；改为每 N 笔或 after_idle 批量 flush（保持 thread-safe）
    4. 手动分类路径保持即时反馈
  </action>
  <verify>
    <automated>pytest test_gui.py test_phase1_integration.py -q</automated>
  </verify>
  <done>Phase 1 全部 11 测试仍通过；progress_interval 生效</done>
</task>

</tasks>

<verification>
- pytest test_gui.py test_phase1_integration.py — 全通过
- 新增 UI 性能回归测试至少 2 项
</verification>

<success_criteria>
- UI-04 满足：大账单自动分类无明显卡顿
- Phase 1 回归无破坏
</success_criteria>

<output>
完成后创建 `.planning/phases/03-gui-module-refactor/03-01-SUMMARY.md`
</output>
