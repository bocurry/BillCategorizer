---
phase: 03-gui-module-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - gui_interface.py
  - gui/__init__.py
  - gui/thread_bridge.py
  - gui/dialogs.py
  - gui/transaction_panel.py
  - gui/results_panel.py
  - gui/interface.py
  - main.py
  - test_gui.py
  - test_phase1_integration.py
autonomous: false
requirements:
  - ARCH-05
user_setup: []

must_haves:
  truths:
    - "gui_interface.py 或 gui/ 包内单文件不超过 500 行"
    - "至少 3 个职责清晰子模块"
    - "from gui_interface import GUIInterface 或 from gui import GUIInterface 对外 API 不变"
    - "Phase 1 run_on_main_thread 与窗口生命周期逻辑完整保留"
    - "分类列表编辑、交易处理、结果预览功能回归正常"
  artifacts:
    - path: gui/interface.py
      provides: "GUIInterface 主类"
    - path: gui/thread_bridge.py
      provides: "ThreadBridgeMixin 或等价"
    - path: gui/dialogs.py
      provides: "DialogMixin"
    - path: gui/transaction_panel.py
      provides: "TransactionPanelMixin"
    - path: gui/results_panel.py
      provides: "ResultsPanelMixin"
  key_links:
    - from: main.py
      to: gui_interface.py
      via: "import GUIInterface"
      pattern: "GUIInterface"
---

<objective>
将 ~1700 行 gui_interface.py 拆分为 gui/ 包内 4-5 个模块，保持 GUIInterface 对外接口与 Phase 1/03-01 行为不变。

Purpose: 满足 ARCH-05，降低后续 Phase 4（总表合并 UI 钩子）改动成本。
Output: gui/ 包 + 薄兼容层 gui_interface.py（re-export）。
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-gui-module-refactor/03-01-SUMMARY.md
@.planning/phases/03-gui-module-refactor/RESEARCH.md
@gui_interface.py
@main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 提取 thread_bridge 与 dialogs 模块</name>
  <files>gui/thread_bridge.py, gui/dialogs.py, gui/__init__.py</files>
  <action>
    1. 创建 gui/ 包
    2. thread_bridge.py：run_on_main_thread、task_queue、_process_queue、_widget_alive、_wait_for_choice_event、show_error/show_info
    3. dialogs.py：_show_modal_dialog、select_bill_source、display_file_list、select_person_mode、select_person_for_transaction、get_validated_input、ask_continue_processing、_prepare_for_continue_dialog
    4. 使用 Mixin 组合或委托；GUIInterface 继承 ThreadBridgeMixin + DialogMixin
    5. 根目录 gui_interface.py 改为 `from gui.interface import GUIInterface` 兼容 re-export
  </action>
  <verify>
    <automated>pytest test_gui.py test_phase1_integration.py -q</automated>
  </verify>
  <done>对话框与线程桥接代码迁出，单文件 &lt; 500 行</done>
</task>

<task type="auto">
  <name>Task 2: 提取 transaction_panel 与 results_panel</name>
  <files>gui/transaction_panel.py, gui/results_panel.py, gui/interface.py</files>
  <action>
    1. transaction_panel.py：交易窗口、display_transaction、classification_menu、add/edit/delete classified
    2. results_panel.py：show_results、_refresh_result_preview
    3. interface.py：GUIInterface 组合所有 Mixin、welcome、run、destroy
    4. 删除原 gui_interface.py 中已迁移代码（保留 re-export 一行或薄包装）
    5. （Deferred，2026-06-08）分类菜单：建议区按 suggestions 增量更新按钮（show/hide + rebind），基础分类 Canvas 首次构建后复用；避免每笔 destroy 整区。验收：连续 2 笔不同 suggestions 的手动分类无明显闪烁；选项 idx 与 get_validated_input 一致
  </action>
  <verify>
    <automated>python -c "from gui_interface import GUIInterface; print('OK')"</automated>
  </verify>
  <done>gui_interface.py 总行数 &lt; 50（re-export）或拆后每模块 &lt; 500 行</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工回归 — GUI 完整流程</name>
  <how-to-verify>
  1. python main.py — 处理 1 个账单（含手动分类 1 笔）
  2. 双击已分类列表编辑分类 — 保存成功
  3. 导出后结果预览正常
  4. 继续处理第二单 — 无卡死、无 treeview 错误
  5. pytest test_gui.py test_phase1_integration.py -q 全通过
  </how-to-verify>
  <done>用户 approved 或描述问题</done>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- 每个 gui/*.py 文件 ≤ 500 行（interface.py 除外可 ≤ 300）
- ROADMAP Phase 3 三条成功标准全部满足
</verification>

<success_criteria>
- ARCH-05 全量满足
- UI-04（03-01）与 ARCH-04（Phase 1）无回归
</success_criteria>

<output>
完成后创建 `.planning/phases/03-gui-module-refactor/03-02-SUMMARY.md`
</output>
